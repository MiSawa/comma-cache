# comma-cache

A tool to manipulate comma choice cache.

## Usage

```bash
comma-cache              # List cache entries
comma-cache list         # List cache entries
comma-cache remove KEY   # Remove a cache entry
```

## Installation

```bash
cargo install comma-cache --release
```

